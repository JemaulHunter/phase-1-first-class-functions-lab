// Code your solution in this file!
const drivers = ['Antonia', 'Nuru', 'Amari', 'Mo'];
const returnFirstTwoDrivers = drivers.slice(0, 2);
const returnLastTwoDrivers = drivers.slice(-2)
displayDrivers.apply(returnFirstTwoDrivers);
    function displayDrivers(){
        
    }
displayDrivers.apply(returnLastTwoDrivers);
    function displayDrivers(){
        
    }
const selectingDrivers = returnFirstTwoDrivers.concat(returnLastTwoDrivers);
